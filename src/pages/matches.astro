---
import Default from '../layouts/Default.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import EmptyState from '../components/EmptyState.astro';
import { getProduct } from '../lib/products';

const product = getProduct('matches');
if (!product) return Astro.redirect('/');

const scope = (Astro.locals as any).scope as string | null;
const showBreadcrumbs = scope === null;
---
<Default title="Matches Status">
  <div class="page-wrap" style="padding-top: 2em; padding-bottom: 3em;">
    {showBreadcrumbs && (
      <Breadcrumbs items={[
        { label: 'Status', href: '/' },
        { label: 'Matches' },
      ]} />
    )}

    <EmptyState label="Coming soon" message="Matches has not launched yet. Status information will appear here when the product goes live." />
  </div>
</Default>
